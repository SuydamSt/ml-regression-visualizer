## cmp-template

## requirements

node.js (lts recommended)
python 3.10+

## setup

\*from the root run:
npm install
npm run install:web
npm run install:api

## run

\*from the root run:
npm run dev
frontend: http://localhost:5173
backend: http://127.0.0.1:8000
